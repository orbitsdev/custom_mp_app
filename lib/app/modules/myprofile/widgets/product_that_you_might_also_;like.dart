import 'package:flutter/material.dart';
import 'package:sliver_tools/sliver_tools.dart';

class ProductThatYouMightAlsoLike extends StatelessWidget {
 ProductThatYouMightAlsoLike({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return MultiSliver(children: [
      //msansory grid leave empty for now 
    ]);
  }
}